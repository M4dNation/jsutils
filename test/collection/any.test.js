require("should");

const any = require("../../lib/collection/all.js");
const isNumber = require("../../lib/object/isNumber.js");

describe("any", function () 
{
    /*it("Should apply function to a collection", function () 
    {
        any([0, 1, true], isNumber).should.be.exactly(true);
    });*/
});