/**
 * Provide identity pattern.
 * @param   {Unknown}   value   The value on which apply identity pattern
 * @returns {Unknown}           The input value.
 */
function identity(value) 
{
    return value;
}

module.exports = identity;